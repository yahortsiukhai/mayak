# ============================================
# Makefile для проекта «Маяк»
# ============================================
# Использование: make КОМАНДА
# Список всех команд: make help
# ============================================

.PHONY: help install up down logs run test lint format clean


# ============================================
# Справка (по умолчанию)
# ============================================
help:  ## Показать все доступные команды
	@grep -E '^[a-zA-Z_-]+:.*?## .*$$' $(MAKEFILE_LIST) | \
		awk 'BEGIN {FS = ":.*?## "}; {printf "  \033[36m%-15s\033[0m %s\n", $$1, $$2}'


# ============================================
# Установка зависимостей
# ============================================
install:  ## Установить Python-зависимости
	pip install -r requirements.txt


# ============================================
# Docker (Postgres + Redis)
# ============================================
up:  ## Запустить Postgres и Redis в фоне
	docker compose up -d

down:  ## Остановить контейнеры
	docker compose down

logs:  ## Смотреть логи контейнеров
	docker compose logs -f


# ============================================
# Приложение
# ============================================
run:  ## Запустить приложение (dev-режим)
	uvicorn app.main:app --reload --host 0.0.0.0 --port 8000


# ============================================
# Тесты и проверки
# ============================================
test:  ## Запустить тесты
	pytest -v

lint:  ## Проверить код линтером
	ruff check app/

format:  ## Отформатировать код
	ruff format app/


# ============================================
# Очистка
# ============================================
clean:  ## Удалить кэш Python
	find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
	find . -type d -name .pytest_cache -exec rm -rf {} + 2>/dev/null || true
	find . -type d -name .ruff_cache -exec rm -rf {} + 2>/dev/null || true